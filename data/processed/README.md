# Processed data

This directory holds rebuildable normalization and preprocessing outputs. Git
ignores every other file here. A file is promoted to `../gameplay/` only after
validation, compaction, documentation, and licensing review.
