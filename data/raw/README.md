# Raw data

Place immutable, versioned upstream downloads here. Git ignores every other
file in this directory. Record the URL, retrieval date, upstream version,
license, and checksum before processing a snapshot.
