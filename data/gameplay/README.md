# Gameplay dataset

This tracked directory will contain the compact, processed dataset needed at
runtime so that a clean clone can eventually be played without downloading the
full OneZoom source database.

No gameplay dataset has been built yet. When one is added, update
`manifest.json` with its version, provenance, license, checksums, schema,
generator version, and reproduction command. Keep large raw and intermediate
artifacts out of this directory.
