#!/usr/bin/env python3
"""Developer entry point for the future OneZoom ingestion pipeline.

Reusable ingestion code belongs in ``phylogenomica.data``. This placeholder is
kept executable so the source-data workflow has a stable entry point while the
actual OneZoom dump schema is being audited.
"""


def main() -> None:
    """Explain the current status of the ingestion command."""
    raise SystemExit(
        "OneZoom ingestion is not implemented yet; see docs/roadmap.md phase 1."
    )


if __name__ == "__main__":
    main()
